# 工作記憶快照

**日期**：2026-05-30  
**專案**：EZRetrofit（`C:\Users\laiis\pg_workspaces\projects\EZRetrofit`）  
**對話 ID**：a8e62f9d-d665-4773-b8af-a8cb8cc8feb1

---

## 本次對話完成事項

### 環境修復
- MCP `settings.json` 中 `context7.tools` 格式錯誤（array → object）→ 移除 `tools` 欄位修復

### Code Review
- 審查 13 支 Java 原始碼，輸出至 `build/reports/code_review.md`
- P0 Bug：Cookie `remove()` key 錯誤（應 `name` 非 `domain`）、`EZRetrofitHelper` race condition
- P1 安全：SHA-1 pinning、弱 cipher、DefaultTestingTrustManager 無防護
- P2 品質：空 catch、join() 邏輯錯誤、命名 typo × 4

### Speckit 流程（已完成 specify + clarify）
- Branch：`fix/ezretrofit-code-quality`
- Spec 目錄：`specs/001-fix-ezretrofit-code-quality/`
- Spec 狀態：✅ 無含糊需求，ready for `/speckit-plan`

### 澄清決策（4 問）
1. **API 改名**：deprecated alias 策略（保留舊名 + 新增正確拼寫）
2. **環境偵測**：`com.github.gmazzo.buildconfig` 插件生成 `BuildConfig.DEBUG`；廢除 `LibConfig`
3. **Logger 機制**：`EZLogger` 可插拔介面（Option C），未注入預設 `System.err`
4. **Cipher 白名單**：OkHttp `ConnectionSpec.MODERN_TLS` 內建清單（Option B）

### 關鍵 FR 摘要
- FR-001/002：Cookie bug 修正（key 錯誤、join 尾端逗號）
- FR-003：EZRetrofitHelper 改為每次 new instance
- FR-004：SHA-256 pinning
- FR-005：BuildConfig.DEBUG 防護 + LibConfig @Deprecated
- FR-006：SSLFactoryManager.build() 拋例外不回 null
- FR-007/011：EZLogger 可插拔介面
- FR-009：MODERN_TLS cipher 白名單
- FR-010：API typo 修正（deprecated alias）

---

## 下一步
- 執行 `/speckit-plan` 產生實作計畫
