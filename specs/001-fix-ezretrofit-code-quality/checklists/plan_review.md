# 檢驗清單：實作計畫需求品質

**目的**: 驗證實作計畫中技術需求的清晰度、完整性與一致性。
**深度**: 標準審查 (預提交 / PR 審查層級)
**聚焦領域**: 安全性、並行性、API 向後相容性、邊界情況

## 需求完整性
- [x] CHK001 - 是否清楚定義了 SSL 設定錯誤時要拋出的具體例外 (Exception) 類型？ [完整性, 規格書 §FR-006]
- [x] CHK002 - 在未注入任何實例時，`EZLogger` 的預設回退行為是否已被明確記錄？ [完整性, 規格書 §FR-011]
- [x] CHK003 - 是否記錄了識別「過期」Cookie 的確切標準？ [完整性, 規格書 §FR-001]

## 需求清晰度
- [x] CHK004 - `EZRetrofit.create()` 所需的執行緒安全邊界是否已被明確指定，以防止競爭危害？ [清晰度, 規格書 §FR-003]
- [x] CHK005 - 「除錯環境」與「發行環境」的定義是否清楚地對照到 `BuildConfig.DEBUG` 值？ [清晰度, 規格書 §FR-005]
- [x] CHK006 - SHA-256 憑證綁定 (Certificate Pinning) 輸入值預期的字串格式（例如 Base64 與 Hex）是否已明確定義？ [清晰度, 規格書 §FR-004]

## 需求一致性
- [x] CHK007 - 向後相容性的需求與引入拼寫正確之新 API 方法的計畫是否一致？ [一致性, 規格書 §FR-010]
- [x] CHK008 - 損毀 Cookie 的日誌記錄需求是否與可插拔的 `EZLogger` 合約一致？ [一致性, 規格書 §FR-007]

## 邊界情況覆蓋
- [x] CHK009 - 當 pin 碼陣列為 null 或空值時，`validateCertificatePin()` 的邊界條件是否已指定？ [邊界情況, 規格書 邊界情況]
- [x] CHK010 - 是否定義了在移除過程中，Cookie 的網域對照表不包含指定網域時的行為？ [邊界情況, 規格書 邊界情況]
- [x] CHK011 - 在 `cancelAll()` 執行過程中，是否定義了處理同時新增呼叫 (calls) 的需求？ [邊界情況, 規格書 邊界情況]
- [x] CHK012 - 當 `PersistentCookieStore` 讀取到格式錯誤或損毀的持久化資料時，是否定義了系統的反應？ [邊界情況, 規格書 使用者場景 5]

## 非功能性需求
- [x] CHK013 - 並行服務建立壓力測試是否指定了效能或時間閾值？ [非功能性需求, 規格書 §SC-002]
- [x] CHK014 - 密碼套件白名單是否嚴格限制為 OkHttp 的 `ConnectionSpec.MODERN_TLS`？ [安全性非功能性需求, 規格書 §FR-009]

## 驗收準則品質
- [x] CHK015 - 向後相容性需求是否可以被客觀量測（例如透過自動化的二進位二階相容性檢查）？ [可量測性, 規格書 §SC-006]
- [x] CHK016 - 「明確的錯誤訊息」之成功準則是否為客觀可驗證，而非主觀判斷？ [可量測性, 規格書 §SC-004]

## 更新後驗證
- [x] CHK017 - SHA-256 pin 碼的 Base64 編碼格式是否明確指定為標準編碼或 URL 安全編碼？ [清晰度, 計畫書 §1]
- [x] CHK018 - `EZLogger` 回退至 `System.err` 的機制是否與 Android 內建的 `Log` 類別指南衝突？ [一致性, 計畫書 §4]
- [x] CHK019 - 並行建立測試的逾時閾值是否已明確定義（例如 < 5 秒）？ [可量測性, 計畫書 §2]
- [x] CHK020 - 是否已明確記錄 `EZLogger` 回退機制必須排他性地使用標準 Java 的 `System.err`，而非 Android 特定的日誌記錄？ [清晰度, 計畫書 §4]

## Java 特有並行性與安全性 (增量項目)
- [x] CHK021 - `EZRetrofit.create()` 的同步機制是否指定使用 Java 標準結構（例如配合 `volatile` 的雙重鎖定檢測）？ [Java 並行性, 計畫書 §2]
- [x] CHK022 - 服務快取的 `ConcurrentHashMap` 是否指定了 Java 型態安全的泛型參數？ [Java 型態安全, 規格書 §關鍵實體]
- [x] CHK023 - 在並行存取期間，執行緒安全的呼叫清單修改失敗時，是否定義了具體的 Java 例外類別？ [Java 例外處理, 規格書 §邊界情況]
- [x] CHK024 - 是否有清晰的 Java 規範驗證 `@Deprecated` 代理方法能保留二進位相容性，且符合正確的 Java 編譯器行為？ [Java 相容性, 規格書 §FR-010]
- [x] CHK025 - `DefaultTestingTrustManager` 中的安全性檢查是否使用標準 Java Security API 與標準的 `SecurityException` 類別實作？ [Java 安全性, 規格書 §FR-005]
- [x] CHK026 - `EZLogger` 回退機制的具體實作類別（例如實作標準介面）是否以 Java 風格指定？ [Java 介面, 規格書 §FR-011]
